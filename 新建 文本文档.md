# EffectKit 自适应背景色工程解析

## 一、项目概述

### 1.1 项目简介

本项目是基于 HarmonyOS 开发的 EffectKit 示例应用，核心功能是通过智能取色技术实现图片自适应背景色效果。应用利用 Image 库和 EffectKit 库中的 ColorPicker 对目标图片进行取色，将获取的颜色作为背景渐变色，并通过 Swiper 组件实现图片轮播，背景色随图片切换平滑过渡。

除核心取色功能外，项目还集成了**隔空投送**、**跨端迁移**、**分布式数据同步**等 HarmonyOS 分布式特性，是一个综合性的 ArkUI 开发示范工程。

### 1.2 核心功能特性

| 功能模块 | 功能描述 |
|---------|---------|
| 智能取色 | 支持6种取色策略（主色调、占比最多、前N颜色、平均色、饱和度最高、智能策略） |
| 自适应背景 | 背景色随图片切换平滑渐变过渡 |
| 图片轮播 | Swiper 组件实现自动/手动轮播 |
| 沉浸式状态栏 | 状态栏颜色随背景色亮度自动适配 |
| 深色模式 | 支持深浅色模式自动适配 |
| 隔空投送 | 支持手势感应分享和碰一碰分享 |
| 跨端迁移 | 支持应用状态在多设备间无缝迁移 |
| 多设备适配 | 支持直板机、折叠屏、平板、2in1 等多种设备形态 |

---

## 二、技术栈与环境

### 2.1 开发技术栈

- **开发语言**: ArkTS (TypeScript 超集)
- **UI 框架**: ArkUI (声明式开发范式)
- **构建工具**: hvigor
- **SDK 版本**: HarmonyOS 6.0.2 Release SDK 及以上
- **目标 SDK**: 6.1.0(23)
- **兼容 SDK**: 6.0.2(22)

### 2.2 运行环境要求

| 环境 | 版本要求 |
|-----|---------|
| HarmonyOS 系统 | 6.0.2 Release 及以上 |
| DevEco Studio | 6.0.2 Release 及以上 |
| 支持设备 | 直板机、平板、PC/2in1、折叠屏、阔折叠、三折叠 |

### 2.3 主要依赖 Kit

| Kit 名称 | 用途 |
|---------|------|
| @kit.ArkUI | UI 组件与窗口管理 |
| @kit.ArkGraphics2D | EffectKit 图形效果（ColorPicker） |
| @kit.ImageKit | 图片处理与 PixelMap |
| @kit.AbilityKit | Ability 生命周期与分布式能力 |
| @kit.BasicServicesKit | 基础服务与设备信息 |
| @kit.ShareKit | 隔空投送与系统分享 |
| @kit.MediaLibraryKit | 媒体库访问 |
| @kit.CoreFileKit | 文件操作 |
| @kit.PerformanceAnalysisKit | 日志输出 (hilog) |

---

## 三、工程结构

### 3.1 整体目录结构

```
effect-kit-master/
├── AppScope/                    # 应用全局配置
├── entry/                       # 主入口模块
│   ├── src/main/
│   │   ├── ets/                 # ETS 源码目录
│   │   │   ├── constants/       # 常量定义
│   │   │   ├── entryability/    # Ability 入口
│   │   │   ├── pages/           # 页面组件
│   │   │   └── utils/           # 工具类
│   │   ├── resources/           # 资源文件
│   │   └── module.json5         # 模块配置
│   ├── build-profile.json5      # 构建配置
│   └── oh-package.json5         # 模块依赖
├── hvigor/                      # 构建工具配置
├── build-profile.json5          # 全局构建配置
├── oh-package.json5             # 项目依赖配置
├── README.md                    # 项目说明
└── LICENSE                      # 开源协议
```

### 3.2 源码目录详解

```
entry/src/main/ets/
├── constants/
│   └── CommonContants.ets          # 全局常量定义
├── entryability/
│   └── EntryAbility.ets            # 应用入口 Ability
├── pages/
│   └── Index.ets                   # 主页面（自适应背景色）
└── utils/
    ├── ColorPickerUtil.ets         # 智能取色工具类
    ├── WindowUtil.ets              # 窗口与状态栏工具类
    ├── DeviceShareUtil.ets         # 隔空投送工具类
    ├── MigrationStateManager.ets   # 跨端迁移状态管理
    ├── DistributedDataManager.ets  # 分布式数据管理
    ├── DeviceManagerUtil.ets       # 设备管理工具
    ├── DeviceUtil.ets              # 设备信息工具
    └── Logger.ets                  # 日志封装工具
```

---

## 四、核心模块解析

### 4.1 主页面模块 (Index.ets)

#### 4.1.1 模块职责

主页面是应用的核心 UI 层，负责：
- 图片轮播展示
- 背景色动态渲染
- 用户交互处理
- 取色结果应用
- 分布式能力集成

#### 4.1.2 核心状态变量

| 变量名 | 类型 | 用途 |
|-------|------|------|
| currentIndex | number | 当前图片索引 |
| currentBgColor | string | 当前背景色（十六进制） |
| targetBgColor | string | 目标背景色（用于动画过渡） |
| isAutoPlay | boolean | 是否自动轮播 |
| colorStrategy | ColorExtractStrategy | 取色策略 |
| imageScale | number | 图片缩放比例 |

#### 4.1.3 关键生命周期

1. **aboutToAppear**: 初始化窗口工具、注册分布式监听、预加载首图颜色
2. **onPageShow**: 注册隔空投送监听、恢复迁移状态
3. **onPageHide**: 注销监听、保存状态
4. **aboutToDisappear**: 资源释放

### 4.2 智能取色模块 (ColorPickerUtil.ets)

#### 4.2.1 模块职责

提供专业的图片取色能力，是整个项目的核心算法模块。

#### 4.2.2 取色策略枚举

```typescript
enum ColorExtractStrategy {
  MAIN_COLOR,           // 主色调
  LARGEST_PROPORTION,   // 占比最多颜色
  TOP_PROPORTION,       // 前N颜色加权
  AVERAGE_COLOR,        // 平均色
  HIGHEST_SATURATION,   // 饱和度最高
  SMART                 // 智能策略（默认）
}
```

#### 4.2.3 核心算法

**智能取色策略 (SMART)**：
- 同时获取主色调、占比最多色、饱和度最高色
- 对每种颜色进行评分（饱和度权重 60%，亮度权重 40%）
- 选择评分最高的颜色作为结果
- 对结果颜色进行优化（提升饱和度、调整亮度范围）

**颜色评分算法**：
```
饱和度评分 = min(1, 饱和度 × 1.5)
亮度评分：
  - 亮度 < 0.3: 亮度 / 0.3 × 0.7
  - 亮度 > 0.7: (1 - 亮度) / 0.3 × 0.7
  - 其他: 1.0
总分 = 饱和度评分 × 0.6 + 亮度评分 × 0.4
```

#### 4.2.4 颜色处理能力

| 方法 | 功能 |
|-----|------|
| extractColorFromPixelMap | 从 PixelMap 提取颜色 |
| colorToHex / hexToColor | 颜色格式转换 |
| calculateLuminance | 计算相对亮度（WCAG 标准） |
| isDarkColor | 判断是否为深色 |
| getContrastColor | 获取对比色 |
| adjustColorForTheme | 深浅模式颜色适配 |
| interpolateColor | 颜色插值（用于动画） |
| rgbToHsl / hslToRgb | RGB/HSL 颜色空间转换 |

### 4.3 窗口工具模块 (WindowUtil.ets)

#### 4.3.1 模块职责

管理窗口状态、沉浸式效果和状态栏样式。

#### 4.3.2 核心功能

1. **沉浸式类型切换**
   - NORMAL: 普通模式，显示系统装饰
   - IMMERSIVE: 沉浸式模式，隐藏装饰，自定义高度 56vp

2. **状态栏内容颜色自适应**
   - 根据背景色亮度自动切换深色/浅色状态栏文字
   - 支持透明状态栏和导航栏

3. **窗口状态监听**
   - 监听窗口最大化/悬浮/普通状态变化
   - 自动切换沉浸式模式

### 4.4 隔空投送模块 (DeviceShareUtil.ets)

#### 4.4.1 模块职责

封装 HarmonyOS 分布式分享能力，提供多种分享方式。

#### 4.4.2 分享类型

| 类型 | 触发方式 | 说明 |
|-----|---------|------|
| GESTURE_SHARE | 手势感应 | 隔空传送，通过手势检测分享意图 |
| KNOCK_SHARE | NFC 接触 | 碰一碰分享，设备接触时触发 |

#### 4.4.3 状态机

```
IDLE → LISTENING → DETECTED → SHARING → SUCCESS/FAILED
```

#### 4.4.4 降级策略

- 检测设备是否支持 HarmonyShare
- 不支持时自动降级为系统分享面板
- 适配不同设备类型（Phone/Tablet/TV/Wearable）

### 4.5 跨端迁移模块 (MigrationStateManager.ets)

#### 4.5.1 模块职责

管理应用在设备间迁移时的状态保存与恢复。

#### 4.5.2 迁移状态数据结构

```typescript
interface MigrationState {
  currentIndex: number;      // 当前图片索引
  imageCount: number;        // 图片总数
  currentBgColor: string;    // 当前背景色
  targetBgColor: string;     // 目标背景色
  colorStrategy: string;     // 取色策略
  imageScale: number;        // 图片缩放比例
  isAutoPlay: boolean;       // 是否自动播放
  swiperInterval: number;    // 轮播间隔
  timestamp: number;         // 时间戳
  sourceDevice?: string;     // 源设备
}
```

#### 4.5.3 核心机制

1. **状态保存** (onContinue)
   - 从 AppStorage 收集当前状态
   - 序列化为 JSON 字符串
   - 存入 wantParam 传递给目标设备

2. **状态恢复** (onCreate/onNewWant)
   - 从 want 参数中提取迁移数据
   - 反序列化并验证有效性
   - 时间戳校验（5分钟过期）
   - 同步到 AppStorage 供页面使用

3. **状态验证**
   - 索引范围校验
   - 缩放比例范围校验
   - 颜色格式校验

### 4.6 常量定义模块 (CommonContants.ets)

#### 4.6.1 核心常量分类

| 类别 | 常量 | 值 | 说明 |
|-----|------|-----|------|
| 布局 | FULL_PARENT | '100%' | 父容器全尺寸 |
| 轮播 | SWIPER_INTERVAL | 3500ms | 自动轮播间隔 |
| 轮播 | SWIPER_DURATION | 500ms | 切换动画时长 |
| 轮播 | SWIPER_ITEM_SPACE | 10 | 条目间距 |
| 动画 | COLOR_TRANSITION_DURATION | 400ms | 颜色过渡时长 |
| 动画 | COLOR_TRANSITION_CURVE | EaseInOut | 过渡曲线 |
| 渐变 | START_GRADIENT_RANGE | 0.0 | 渐变起始位置 |
| 渐变 | END_GRADIENT_RANGE | 0.5 | 渐变结束位置 |
| 加载 | IMAGE_LOAD_TIMEOUT | 10000ms | 图片加载超时 |
| 加载 | LOADING_BG_COLOR | '#FFF5F5F5' | 加载中背景色 |
| 加载 | ERROR_BG_COLOR | '#FFE0E0E0' | 加载失败背景色 |

### 4.7 日志工具模块 (Logger.ets)

#### 4.7.1 模块职责

封装 hilog 日志系统，提供统一的日志输出接口。

#### 4.7.2 日志级别

- debug: 调试信息
- info: 一般信息
- warn: 警告信息
- error: 错误信息

---

## 五、核心功能实现原理

### 5.1 自适应背景色实现

#### 5.1.1 整体流程

```
图片切换触发 → 异步取色 → 颜色优化 → 启动过渡动画 → 背景色平滑更新
```

#### 5.1.2 关键技术点

1. **取色时机**：在 Swiper 的 `onAnimationStart` 事件中触发取色，确保切换动画开始时就开始准备颜色

2. **异步处理**：取色操作异步执行，避免阻塞 UI 线程

3. **平滑过渡**：使用 `animateTo` 启动属性动画，实现颜色渐变效果
   - 动画时长: 400ms
   - 动画曲线: EaseInOut

4. **渐变渲染**：使用 `linearGradient` 背景属性
   - 方向: 从顶部到底部
   - 范围: 0.0 ~ 0.5（上半部分渐变）

### 5.2 智能取色策略详解

#### 5.2.1 为什么需要智能策略

简单的平均色或主色调存在以下问题：
- 平均色容易偏灰，视觉效果不佳
- 主色调可能饱和度过低
- 占比最多的颜色可能是背景色（如白色）

#### 5.2.2 智能策略工作原理

1. **多维度采样**：同时获取三种候选颜色
   - 主色调 (MainColor)
   - 占比最多色 (LargestProportionColor)
   - 饱和度最高色 (HighestSaturationColor)

2. **综合评分**：
   - 饱和度评分（权重 60%）：确保颜色鲜艳
   - 亮度评分（权重 40%）：避免过暗或过亮

3. **颜色优化**：
   - 饱和度提升 20% + 10% 基础值
   - 亮度限制在 0.3 ~ 0.8 范围内
   - 确保颜色在视觉上舒适且有辨识度

### 5.3 沉浸式状态栏实现

#### 5.3.1 实现原理

1. **透明状态栏**：设置状态栏颜色为完全透明
2. **内容颜色自适应**：根据背景色亮度自动切换深色/浅色文字
3. **窗口装饰控制**：隐藏系统窗口装饰，实现真正沉浸式

#### 5.3.2 亮度计算标准

采用 WCAG 2.0 相对亮度计算公式：
```
L = 0.2126 × R + 0.7152 × G + 0.0722 × B
```
其中 R、G、B 为经过 gamma 校正的线性值。

亮度 > 0.5 视为浅色背景，使用深色状态栏文字；
亮度 ≤ 0.5 视为深色背景，使用浅色状态栏文字。

### 5.4 跨端迁移实现

#### 5.4.1 迁移触发流程

```
用户发起迁移 → onContinue 保存状态 → 系统传输数据 → 目标设备启动
→ onCreate/onNewWant 恢复状态 → 页面渲染迁移后状态
```

#### 5.4.2 关键技术点

1. **状态持久化**：使用 JSON 序列化，确保数据可传输
2. **时效性控制**：5 分钟过期机制，防止使用过期状态
3. **数据验证**：恢复时验证数据有效性，避免异常
4. **状态同步**：通过 AppStorage 在 Ability 和 Page 间共享状态

### 5.5 隔空投送实现

#### 5.5.1 两种分享模式

1. **手势感应分享 (GesturesShare)**
   - 通过手势传感器检测用户分享意图
   - 无需物理接触，体验更自然

2. **碰一碰分享 (KnockShare)**
   - 通过 NFC 近场通信触发
   - 设备轻轻一碰即可分享

#### 5.5.2 数据配置

支持多种数据类型：
- 纯文本
- URI 链接
- 文件 URI
- 统一数据类型 (UTD)

---

## 六、资源配置

### 6.1 资源目录结构

```
resources/
├── base/              # 默认资源
│   ├── element/       # 元素资源（颜色、字符串、整数）
│   ├── media/         # 媒体资源（图片、图标）
│   └── profile/       # 配置文件（页面路由）
├── en_US/             # 英文国际化
│   └── element/
└── zh_CN/             # 中文国际化
    └── element/
```

### 6.2 主要资源文件

| 文件 | 用途 |
|-----|------|
| color.json | 颜色资源定义 |
| string.json | 字符串资源（支持多语言） |
| integer.json | 整数资源 |
| main_pages.json | 页面路由配置 |
| image1~8.png | 示例图片（8张） |
| startIcon.png | 启动图标 |
| layered_image.json | 分层图标配置 |

### 6.3 页面路由配置

`main_pages.json` 中定义应用页面路由：
```json
{
  "src": [
    "pages/Index"
  ]
}
```

---

## 七、构建配置

### 7.1 全局构建配置 (build-profile.json5)

```json5
{
  app: {
    products: [
      {
        name: 'default',
        compatibleSdkVersion: '6.0.2(22)',
        targetSdkVersion: '6.1.0(23)',
        runtimeOS: 'HarmonyOS'
      }
    ],
    buildModeSet: [
      { name: 'debug' },
      { name: 'release' }
    ]
  }
}
```

### 7.2 模块配置

- **模块名**: effectkitsample
- **模块类型**: entry（主入口模块）
- **源码路径**: ./entry

---

## 八、权限说明

### 8.1 申请的权限列表

| 权限名称 | 用途 | 使用场景 |
|---------|------|---------|
| ohos.permission.READ_IMAGEVIDEO | 读取图片和视频 | 从相册选取图片取色 |
| ohos.permission.DISTRIBUTED_DATASYNC | 分布式数据同步 | 跨设备数据同步 |
| ohos.permission.ACCESS_SERVICE_DM | 访问设备管理服务 | 设备发现与连接 |
| ohos.permission.DISTRIBUTED_SOFTBUS_CENTER | 分布式软总线中心 | 分布式通信 |

### 8.2 权限使用说明

所有权限均为 `inuse` 级别，仅在使用时申请，符合 HarmonyOS 权限最佳实践。

---

## 九、模块配置 (module.json5)

### 9.1 基本信息

- **模块名**: effectkitsample
- **模块类型**: entry
- **主入口**: EntryAbility
- **安装方式**: 随应用安装
- **免安装**: 不支持

### 9.2 支持设备类型

- phone（手机）
- tablet（平板）
- 2in1（二合一设备）
- tv（电视）
- wearable（穿戴设备）

### 9.3 Ability 配置

| 配置项 | 值 | 说明 |
|-------|-----|------|
| name | EntryAbility | Ability 名称 |
| exported | true | 可被外部调用 |
| continuable | true | 支持跨端迁移 |
| launchType | multiton | 多实例启动模式 |
| skills | action.system.home | 支持桌面启动 |

---

## 十、高性能与优化要点

### 10.1 性能优化

1. **异步取色**：取色操作在异步线程执行，不阻塞 UI
2. **颜色缓存**：避免重复计算相同图片的颜色
3. **平滑动画**：使用属性动画而非逐帧重绘
4. **懒加载**：图片按需加载，减少内存占用

### 10.2 代码质量

1. **单一职责**：每个工具类只负责一个功能领域
2. **单例模式**：状态管理器使用单例，确保全局唯一
3. **错误处理**：所有异步操作都有完整的异常捕获
4. **类型安全**：充分利用 TypeScript 类型系统
5. **常量集中**：所有魔法数字集中在常量文件

### 10.3 可扩展性

1. **策略模式**：取色策略可灵活扩展
2. **接口抽象**：通过接口定义数据结构，便于扩展
3. **模块化设计**：各功能模块解耦，可独立复用

---

## 十一、约束与限制

1. **系统版本**: 仅支持 HarmonyOS 6.0.2 Release 及以上
2. **设备类型**: 标准系统设备，不支持轻量系统
3. **分布式能力**: 需要登录相同华为账号并开启分布式能力
4. **隔空投送**: 需要设备支持相应的硬件传感器
5. **图片格式**: 取色功能依赖 PixelMap，支持常见图片格式

---

## 十二、总结

本项目是一个高质量的 HarmonyOS 示范工程，具有以下特点：

1. **核心技术示范**: 深入展示 EffectKit ColorPicker 的使用方法和高级技巧
2. **分布式特性**: 完整实现跨端迁移、隔空投送等 HarmonyOS 特色功能
3. **代码质量高**: 结构清晰、注释完善、遵循最佳实践
4. **用户体验优**: 沉浸式设计、平滑动画、深色模式适配
5. **多端适配**: 支持多种设备形态和屏幕尺寸

该工程可作为学习 ArkUI 开发、EffectKit 图形效果、HarmonyOS 分布式特性的优秀参考案例。
